module gcrisp::engine::script::props;

import gcrisp::engine::script;
import raylib5::rl;

struct Character
{
  Texture2D texture;
  String name;
}

struct Background
{
  Texture2D texture;
}

fn void Character.update(&self)
{
  
}
